# Python
__pycache__/
*.pyc
venv/
env/

# Flask instance folder, this is where the SQLite database file lives.
# Never commit the actual database, everyone should be running against
# their own local copy while developing.
instance/

# Environment variables, secrets live here locally, never in git.
.env

# OS noise
.DS_Store
Thumbs.db
